connectionConfig:
  replicaSet:
    - id: "orion-server1"
      endpoint: "http://127.0.0.1:6001"
  rootCAs: "../crypto/CA/CA.pem"

sessionConfig:
  userConfig:
    userID: "controller"
    certPath: "../crypto/controller/controller.pem"
    privateKeyPath: "../crypto/controller/controller.key"
  txTimeout: 20s
  queryTimeout: 10s
